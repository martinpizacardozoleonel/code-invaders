services:
  - type: web
    name: code-invaders
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /api/leaderboard
    autoDeploy: true
    envVars:
      - key: DATABASE_URL
        sync: false
